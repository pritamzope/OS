# shell file to clean compiled code

# remove all .o object files
rm *.o

# remove isodir
rm -r isodir

# remove VGA_Colors_OS.bin
rm VGA_Colors_OS.bin

# remove VGA_Colors_OS.iso
rm VGA_Colors_OS.iso
