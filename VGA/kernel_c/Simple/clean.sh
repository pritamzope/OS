# shell file to clean compiled code

# remove all .o object files
rm *.o

# remove isodir
rm -r isodir

# remove VGA_Simple_OS.bin
rm VGA_Simple_OS.bin

# remove VGA_Simple_OS.iso
rm VGA_Simple_OS.iso
